function greeting(name="Jia"){ //if name not provided as an argument while calling the function then Jia will be the name else the given name will be printed
    console.log(`Hello! ${name}`);
    
}
greeting("Bar")